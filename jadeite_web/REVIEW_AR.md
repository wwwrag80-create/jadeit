# تقرير المراجعة الشاملة — نظام جاديت

مراجعة سطراً بسطر لكل أجزاء النظام: تطبيق الويب (Flutter)، قاعدة البيانات (Supabase)،
برنامج سطح المكتب ومحرك مزامنته (Python)، وأداة الترحيل. كل خطأ مذكور هنا **أُعيد إنتاجه
فعلياً** قبل إصلاحه (على PostgreSQL 16 حقيقي، وببناء Flutter فعلي، وبتشغيل محرك المزامنة)،
ثم أُضيف اختبار يمنع رجوعه.

---

## ⛔ أولاً: إجراءات عاجلة عليك أنت (لا يستطيع الكود فعلها)

1. **ألغِ المفتاح السري فوراً وأنشئ غيره.** المستودع `wwwrag80-create/jadeit` **عام**، وملف
   `jadeite_web.zip` المرفوع فيه يحتوي `SUPABASE_SECRET_KEY = "sb_secret_m1ZB…"` داخل
   `rageh-1-34-14-cloud.py`. هذا المفتاح يتخطى كل الحمايات: أي شخص يستطيع به قراءة وتعديل
   وحذف بيانات **كل** عملائك.
   - Supabase ← **Settings ← API Keys ← Secret keys** ← احذف المفتاح المكشوف وأنشئ جديداً.
   - ضع الجديد في ملف `desktop_sync/admin_secret.key` على جهازك فقط (لا يُرفع لـ git).
   - حذف الملف من المستودع **لا يكفي** — المفتاح باقٍ في تاريخ git. الإلغاء هو الحل الوحيد.
2. **شغّل `supabase/INSTALL_ALL.sql` الجديد** في SQL Editor — آمن على قاعدتك الحالية
   (مُختبر على قاعدة بالنسخة القديمة ببياناتها)، ويغلق ثغرات العزل المذكورة أدناه.
   في آخر نتائجه جدول **«كل دالة ما زال anon يستطيع تنفيذها»** — راجعه (انظر البند ٦ أدناه).
3. **أعد بناء exe العميل** من `rageh-CLIENT.py` الجديد ووزّعه (يصلح أخطاء المزامنة أدناه).
4. غيّر كلمة مرور لوحة المدير المحلية (`admin/admin`) بمتغيّر البيئة `JADEITE_ADMIN_PASSWORD`.

---

## ثانياً: فهم النظام — كيف يعمل كله معاً

```
 جهاز العميل (exe)                    Supabase                         المدير
┌───────────────────────┐        ┌──────────────────────┐        ┌────────────────────┐
│ rageh-CLIENT.py        │ رفع   │ transactions (دفتر   │  قراءة │ لوحة الويب Flutter  │
│ SQLite محلية = الحقيقة │──────►│ الأستاذ لكل المصانع) │◄──────│ (جوال/متصفح)        │
│ محفّزات → sync_outbox  │ RPC   │ + RLS + دوال RPC      │  JWT   │ انتحال شخصية العميل │
│ cloud_sync (كل ١٠ ث)   │ anon+ │ tenant_id لكل صف      │        ├────────────────────┤
│ لا يسحب أبداً          │ رمز   │                      │ سحب   │ rageh-1-34-14-cloud │
└───────────────────────┘ مزامنة └──────────────────────┘──────►│ (مرآة قراءة فقط)    │
                                                                 └────────────────────┘
```

- **المستأجر (tenant) = مصنع.** كل صف في كل جدول يحمل `tenant_id`، والعزل مفروض بـ RLS
  وبفحص `has_tenant_access()` داخل كل دالة.
- **كل شيء حركة** في جدول واحد `transactions`: وارد، مبيعات، صرف/قبض عمال، صناديق خياس،
  قيود يومية، قيود افتتاحية. الأرصدة كلها تُحسب في قاعدة البيانات (`treasury_balance`،
  `box_period_totals`، `worker_ledger`…) لتعطي رقماً واحداً لكل الأجهزة.
- **الفترة المحاسبية (period)** قرار مستقل عن التاريخ: البرنامج يسجّل التاريخ الحقيقي ويختم
  الحركة بالفترة المعروضة وقت التسجيل.
- **حالات الحركة:** `ACTIVE` و`SETTLED_INOUT` تُحتسبان؛ `SETTLED` (قسم أُقفلت فترته) و`MEMO`
  (سطر معلوماتي: خياس البوليش/المركب/صافي الطقم) لا تُحتسبان.
- **المزامنة باتجاه واحد** (منذ حادثة `RECOVERY_AR.md`): العميل يرفع فقط، والمدير يسحب فقط.
  برنامج العميل يرقّم حركاته محلياً (`invoice_id`) ويرفعها بالرقم نفسه (`seq_no`) بـ upsert.
- **الدخول:** العميل يدخل بجدول `clients` القديم عبر `client_login_full` (مفتاح anon) فيحصل
  على `sync_token` في الذاكرة فقط؛ المدير في الويب يدخل بـ Supabase Auth ودور `admin`.

---

## ثالثاً: ما وُجد وما أُصلح

### 🔴 أمان — حرج

| # | المشكلة | الأثر الفعلي (مُثبت بالاختبار) | الإصلاح |
|---|---|---|---|
| ١ | المفتاح السري `sb_secret_…` مكتوب داخل كود نسخة المدير، والمستودع عام | وصول كامل لكل بيانات كل العملاء لأي شخص | أُزيل من الكود؛ يُقرأ من `admin_secret.key` أو متغيّر بيئة. `final_audit.py` يفشل الآن لو وُجد أي مفتاح سري حرفي. **الإلغاء مطلوب منك** |
| ٢ | ١٠ دوال قراءة `SECURITY DEFINER` بلا أي فحص للمستأجر (`treasury_balance`، `search_transactions`، `invoice_archive`، `journal_entries`، `losses_breakdown`…) | عميل (أ) قرأ رصيد وحركات وفواتير عميل (ب) كاملة | كل دالة تتحقق الآن من `has_tenant_access` |
| ٣ | الدوال نفسها كانت متاحة لـ **anon** (المفتاح العام الموجود في الويب وفي exe) | أي زائر بلا تسجيل دخول قرأ حركات أي مصنع بمعرّفه | `14_security_hardening.sql`: anon لا يستدعي إلا دوال المزامنة المحمية برمز |
| ٤ | `ensure_tenant_link` و`diagnose_client_sync` متاحتان لأي مستخدم مسجّل وترجعان **رمز مزامنة** أي مصنع | عميل يأخذ رمز مصنع آخر ← يرفع/يحذف حركاته عبر `sync_push/delete` | محصورتان بمفتاح الخدمة وSQL Editor، والتشخيص لا يعرض الرمز |
| ٥ | `run_maintenance` / `purge_audit_log` / `purge_sync_deletions` متاحة لـ anon | أي زائر يحذف سجل التدقيق | للمدير فقط |
| ٦ | محفّز حماية صف المصنع لا يحمي `sync_enabled`/`sync_token` | العميل يعيد تفعيل مزامنة أوقفها المدير بنفسه | المحفّز يحميها الآن |
| ٧ | `client_login_full` متاحة لـ anon بلا حد لمحاولات التخمين | تخمين كلمات المرور بلا قيود | جدول `login_attempts`: ١٠ محاولات فاشلة/١٥ دقيقة توقف الاسم مؤقتاً |

### 🔴 قاعدة البيانات — أعطال فعلية

| # | المشكلة | الإصلاح |
|---|---|---|
| ٨ | `INSTALL_ALL.sql` **يفشل على أي قاعدة جديدة**: `generation expression is not immutable` (عمود period المولَّد) — فنصيحة «أنشئ مشروعاً جديداً وشغّل الملف» في `SETUP_AR.md` لا يمكن أن تنجح | عمود عادي + محفّز `fill_txn_period` |
| ٩ | بعد `13_fix_admin_mirror.sql` صارت **كل حركة تُسجَّل من الويب بفترة فارغة** — فتختفي من كل الشاشات والأرصدة (مُثبت: حركة الويب رجعت period = NULL) | المحفّز يملؤها + الويب يرسل الفترة صراحةً + تصحيح الحركات القديمة الفارغة تلقائياً |
| ١٠ | إعادة تشغيل `INSTALL_ALL.sql` تفشل: `cannot remove parameter defaults` (رغم أن الوثائق تقول «آمن لإعادة التشغيل») | توحيد تعريف `assert_sync_token`؛ الاختبار الآلي يثبّت الملف مرتين |
| ١١ | `write_audit()` تكتب في أعمدة غير موجودة (`actor`، `old_data`…) وتحوّل رقم الحركة لـ uuid — ولم تكن مفعّلة أصلاً، فكل دورة رفع من سطح المكتب تضيف نسختين كاملتين لكل صف في سجل التدقيق | أُصلحت وفُعّلت: التعديل يُسجَّل بالفرق فقط، والرفع بلا تغيير لا يُسجَّل |
| ١٢ | حركات الويب تأخذ رقم `max+1`، وأول حركة يسجّلها العميل على جهازه تأخذ الرقم نفسه ← الرفع (upsert) **يمحو حركة الويب بصمت** | حركات الويب لمصانع سطح المكتب في نطاق مستقل يبدأ من مليار |
| ١٣ | حالة `MEMO` غير موجودة في السحابة، فتُرفع السطور المعلوماتية كـ `ACTIVE` ← **تدخل خطأً في رصيد الخزينة وصناديق الخياس عند المدير** | أُضيفت `MEMO`؛ المحرك يرفعها كما هي (ومع سحابة قديمة يحوّلها لـ `SETTLED` بدل `ACTIVE`) |
| ١٤ | الويب يحتسب `ACTIVE` فقط، والبرنامج يحتسب `ACTIVE` + `SETTLED_INOUT` ← أرقام المدير ≠ أرقام العميل | توحيد الفلتر في كل الدوال والاستعلامات |
| ١٥ | رصيد الخزينة في السحابة يتجاهل `رصيد افتتاحي` و`صادر ذهب` و`صرف خياس مقفل` التي يحتسبها البرنامج | أُضيفت (مطابقة لـ `treasury_effect` في البرنامج) |
| ١٦ | حذف قيد إقفال صندوق من شاشة القيود يترك الصندوق «مقفلاً» بلا قيد | ممنوع — يُلغى من شاشة الإقفال فقط |
| ١٧ | المدير المتصفّح لحساب عميل يجعل العميل يظهر «متصلاً الآن» في لوحة المتابعة | نبض الحضور للعميل فقط (في الويب وفي قاعدة البيانات) |

### 🔴 برنامج سطح المكتب — أعطال فعلية

| # | المشكلة | الإصلاح |
|---|---|---|
| ١٨ | `cloud_sync.py` يستدعي `_sqlite_dt` غير المعرّفة ← **كل دورة سحب تسقط بـ NameError**، فيتحوّل الرفع للتراجع التدريجي (حتى ٥ دقائق بدل ١٠ ثوانٍ) وتظهر عند المدير رسالة خطأ دائمة | استيراد الدالة من `sync_down` |
| ١٩ | **المحرك يسحب من السحابة ويكتب في قاعدة العميل كل ١٥ ثانية** رغم قاعدة «العميل لا يسحب أبداً» (`RECOVERY_AR.md`) — الشيء الوحيد الذي كان يوقفه هو الخطأ (١٨)، وحذف الحركات كان يمرّ فعلاً | السحب معطّل افتراضياً (`pull_enabled=False`)، واختبار سلوكي يثبت أن قاعدة العميل لا تتغيّر |
| ٢٠ | إزالة التكرار قبل الرفع مكتوبة **بعد `return`** فلا تُنفَّذ أبداً (والاختبار القديم كان ينجح لأنه يبحث عن النص فقط) | أُصلحت واختُبرت فعلياً |
| ٢١ | التواريخ المسحوبة تُقصّ إزاحتها بدل تحويلها ← نسخة المدير تعرض الوقت متأخراً ٣ ساعات، وحركات ما بعد منتصف الليل **بيوم خاطئ** | تحويل صحيح لتوقيت الجهاز |
| ٢٢ | `login_screen.py`: رسالة الخطأ تُعرض عبر lambda تشير لمتغيّر يحذفه بايثون ← الشاشة تعلق بلا رسالة | أُصلحت |
| ٢٣ | `make_client_build.py` يستبدل `IS_ADMIN_BUILD = True` داخل التعليقات أيضاً | الاستبدال لسطر التعريف فقط، مع تحقق عدم وجود أي مفتاح سري |
| ٢٤ | `install_sync_schema` ينشئ جدول الحركات بلا عمود `period` | يُضاف تلقائياً |
| ٢٥ | **نسخة المدير كانت ترفع نسختها للسحابة**: عند فتح حساب عميل من برنامج المدير كانت ترفع قاعدتها كل ١٠ دقائق مكان نسخة العميل الاحتياطية على السحابة — مخالفةً لقاعدة «المدير يقرأ فقط». وشاشة النسخ في نسخة العميل فيها زر «استرجاع من السحابة» يخالف قاعدة «العميل لا يسحب». وشاشة الرفع عند الدخول تُنشئ ملف قاعدة فارغاً حين لا يوجد، فلا يُسترجع من مجلد النسخ على الجهاز | المدير لا يرفع شيئاً؛ العميل لا يسحب شيئاً (أُخفي الزر، والتنزيل عند الفتح/التلف لنسخة المدير وحدها)؛ ملف البيانات المفقود يُسترجع من أحدث نسخة سليمة في `Backups` على جهاز العميل نفسه. مجلدات العميل وأسماء ملفاته **كما هي** (`test_reinstall_restore.py`) |

### 🟠 تطبيق الويب

| # | المشكلة | الإصلاح |
|---|---|---|
| ٢٥ | **البناء يفشل**: خطا Cairo المطلوبان في `pubspec.yaml` غير موجودين (والـ PDF العربي يعتمد عليهما)، وقيد `intl ^0.20.2` يتعارض مع Flutter 3.27.1 المثبّت في `build_web.sh` | أُضيف خطا Cairo (رخصة OFL) + قيد intl متوافق. **البناء الآن ينجح** (`flutter build web`) |
| ٢٦ | ترحيل الفاتورة سطراً سطراً: انقطاع الاتصال في المنتصف يترك فاتورة ناقصة | دالة `post_transactions_batch` — الفاتورة كلها في معاملة واحدة |
| ٢٧ | **تعديل فاتورة = حذف ثم إعادة تسجيل** بطلبين منفصلين: فشل الثاني يمحو الفاتورة | استبدال ذرّي في معاملة واحدة، ويخضع لقفل التعديل، ويرفض لو تغيّرت الفاتورة من جهاز آخر |
| ٢٨ | الحركة تُسجَّل في فترة **تاريخها** لا الفترة المعروضة: تغيير الفترة ثم التسجيل يجعل الحركة «تختفي» | الفترة المعروضة تُرسل صراحةً مع كل حركة (مثل البرنامج) |
| ٢٩ | التاريخ يُرسل بلا منطقة زمنية فيُخزَّن كأنه UTC ← يظهر متأخراً ٣ ساعات وقد يتغيّر اليوم | يُرسل لحظةً كاملة بتوقيت UTC |
| ٣٠ | على الجوال **لا يوجد زر خروج** ولا حالة القفل للعميل | أُضيفت أسفل القائمة الجانبية |
| ٣١ | شاشة المبيعات تمنع الترحيل عند قفل التعديل، بخلاف القاعدة الموثّقة وبقية الشاشات | الترحيل الجديد مسموح؛ تعديل المرحّل فقط مقفول |
| ٣٢ | شريط انتحال الشخصية يَعِد المدير بأن «تعديلك يصل جهاز العميل خلال ثوانٍ» — غير صحيح منذ المزامنة أحادية الاتجاه | رسالة صادقة: التعديل يغيّر نسخة السحابة فقط |
| ٣٣ | كشف الحساب يبدأ كل شهر من صفر | سطر «رصيد أول المدة» مُرحَّل من الفترات السابقة |
| ٣٤ | أخطاء إجراءات المدير (إنشاء/حذف/قفل) والطباعة ترمي استثناءات صامتة | رسائل واضحة لكل إجراء |
| ٣٥ | انتهاء الجلسة أو الخروج من تبويب آخر يترك الشاشات مفتوحة وكل طلباتها تفشل | الرجوع لشاشة الدخول تلقائياً |
| ٣٦ | عميل حسابه غير مربوط بمصنع أو موقوف يرى نظاماً فارغاً | شاشة توضّح السبب مع زر خروج |
| ٣٧ | تنسيق التواريخ بلغة `en` يرمي `LocaleDataException` قبل تحميل الترجمات | `en_US` المدمجة دائماً |
| ٣٨ | البث اللحظي يحدّث الخزينة والفواتير فقط؛ بقية الشاشات المفتوحة تبقى قديمة | عدّاد إصدار بيانات تراقبه كل الشاشات |
| ٣٩ | رسائل أخطاء قاعدة البيانات العربية تظهر ملفوفة في `PostgrestException(message: …)` | تُعرض الرسالة العربية نفسها |

### 🟡 أداة الترحيل
- `MEMO` كانت تُرسل فتفشل الدفعة (أو تتحوّل لـ `ACTIVE`)؛ والفترة الصريحة لا تُرحَّل؛
  والتواريخ بلا إزاحة توقيت (تظهر متأخرة ٣ ساعات). أُصلحت الثلاثة، ويُحفظ القسم الحقيقي
  للحساب (`category_raw`).

### 🔴 الفترات المحاسبية والتقرير الشهري (المرحلة الثانية)

**القاعدة المعتمدة الآن في البرنامج والويب معاً:**
الحركة تنتمي للفترة التي كنت تعمل عليها وقت تسجيلها (عمود `period`) أياً كان تاريخها،
وكل فترة تبدأ تلقائياً برصيد نهاية الفترة التي قبلها بالضبط.

**العَرَض الذي أبلغت عنه:** التقرير الشهري يطابق الشاشة في فترة ٨، وعند الانتقال لفترة ٩
تختلف أرقامه. السبب أن **ثلاث شاشات كانت تحسب الرصيد بثلاث معادلات مختلفة**:

| # | الخطأ | أثره |
|---|---|---|
| ١ | رصيد أول المدة كان يخصم خياس المصنعين/المركبين **غير المُقفل** فقط | إقفال خياس فترة ٨ (قيد بين «حساب الخسائر» والصندوق لا يمس الخزينة) كان **يرفع رصيد افتتاح فترة ٩ بقيمة المُقفل** — والبرنامج يطلب هذا الإقفال عند الانتقال للفترة الجديدة |
| ٢ | التقرير الشهري بمعادلة خاصة: ACTIVE فقط، والذهب المسترجع من الصناديق يُحسب **مرتين** (وارداً وخصماً من الخياس)، بلا «صادر ذهب» ولا قيود الخزينة، وبداية أول فترة من القيود الافتتاحية المعلَّمة فقط | يختلف عن الشريط في أي فترة فيها صادر أو مسترجع أو قيد خزينة |
| ٣ | شريط الخزينة: «رصيد افتتاحي» كان **يمحو** الرصيد الجاري بدل أن يُضاف له، وقيود «حساب الخزينة» اليومية تُجمع من **كل الفترات** وتُضاف لأي فترة معروضة | رقم الفترة يتأثر بقيود فترات أخرى |
| ٤ | كشف الحساب يفلتر بشهر **التاريخ** لا بالفترة، ويبدأ من الصفر | حركة تاريخها ٩/١ سُجّلت على فترة ٨ تظهر في كشف ٩ |
| ٥ | بعد الحفظ تظهر رسالة «رُحّلت الحركة لفترة تاريخها ولن تظهر في الفترة الحالية» | عكس ما يحدث فعلاً — أُزيلت |
| ٦ | فحص تكرار رقم الصف يبحث في فترة شهر التاريخ لا في الفترة التي ستُسجَّل فيها الحركة | تكرار يمر، أو رفض خاطئ |
| ٧ | الويب: رصيد الخزينة لا يخصم خياس المصنعين/المركبين، ولا يعرف الصناديق المضافة من البرنامج، وأسماء المسترجع فيه (`مسترجع الكاستنج`) تختلف عن البرنامج (`مسترجع كاستنج`) | رقم المدير في الويب ≠ رقم العميل في البرنامج |

**الإصلاح — مصدر واحد:**
- البرنامج: `treasury_bucket` يصنّف كل حركة مرة واحدة، و`get_treasury_ledger` يبني كل فترة
  منه (بداية = نهاية السابقة). **الشريط، رصيد أول المدة، التقرير الشهري، وكشف حساب الخزينة
  كلها تقرأ منه** — فيستحيل أن يختلف رقم شاشة عن رقم التقرير.
- الخياس الفعلي يبقى مخصوماً من الخزينة في فترته **قبل الإقفال وبعده** (الإقفال إعادة تصنيف
  للفاقد لا ذهبٌ عاد). والإقفال القديم بالأرشفة يعطي الرصيد نفسه بلا خصم مزدوج.
- التقرير الشهري: عمود جديد «قيود الخزينة ±»، والفترة المعروضة مميّزة، والأرقام نفسها من أي
  فترة فُتح.
- كشف الحساب بالفترة، ويبدأ بسطر «رصيد أول المدة» المُرحَّل (لكل الحسابات لا الخزينة فقط).
- الويب: `15_period_ledger.sql` ينقل المعادلة نفسها (`treasury_period_ledger` +
  `section_actual_khayas`)، و«رصيد الخزينة» في الويب يُقرأ منه، والتقرير الشهري في الويب
  فيه جدول «رصيد الخزينة لكل الفترات» بالأعمدة نفسها.
- **الإثبات:** `test_period_ledger.py` (يشغّل دوال البرنامج الحقيقية) و
  `supabase/tests/20_period_ledger_tests.sql` يستخدمان **البيانات نفسها** ويتوقعان **الأرقام
  نفسها** (نهاية ٨ = 983، نهاية ٩ = 1018) — رقم واحد في البرنامج والويب.

> ملاحظة للأرقام القديمة: بعد التحديث قد يتغيّر رصيد افتتاح الفترات التي أُقفل فيها خياس
> المصنعين/المركبين (كان مرتفعاً خطأً بقيمة المُقفل)، ويتغيّر التقرير الشهري حيث كان المسترجع
> يُحسب مرتين. الأرقام الجديدة هي الصحيحة محاسبياً وتطابق الشريط.

**للتحديث على Supabase:** شغّل `supabase/15_period_ledger.sql` في SQL Editor (أو `INSTALL_ALL.sql` كاملاً).

### 🔴 برنامج المدير يطابق جهاز العميل حرفياً (المرحلة الثالثة)

**القاعدة:** جهاز العميل ← السحابة ← برنامج المدير. العميل **يرفع فقط** ويستعيد من مجلد
النسخ على جهازه، والمدير **يقرأ فقط**. مجلدات العميل وأسماء ملفاته لم تتغيّر.

| # | ما كان يجعل شاشة المدير تختلف عن العميل | الإصلاح |
|---|---|---|
| ١ | **ترتيب الأسماء**: العميل يعرض العمال والموردين بترتيب إضافتهم، والسحابة ترجعهم **أبجدياً** | يُرفع ترتيب كل اسم (`desktop_order`) ويُسحب به |
| ٢ | **الإعدادات لم تُرفع إطلاقاً**: نسب استرجاع الخياس (تغيّر أرقام ربح/خسارة الطقم)، أسماء الأعمدة المخصّصة، ترتيب الشاشات والأقسام، تلوين السالب | لقطة كاملة من إعدادات العميل تُرفع عند أي تغيير وتُسحب للمدير |
| ٣ | **التاريخ**: المدير يحوّل وقت الحركة لتوقيت جهازه، فلو اختلف ضبط التوقيت اختلفت الساعة واليوم | يُرفع نص التاريخ كما هو عند العميل ويعرضه المدير حرفياً |
| ٤ | نسخة المدير كانت ترفع نسختها للسحابة مكان نسخة العميل الاحتياطية | المدير لا يرفع شيئاً |

**الإثبات:** `test_admin_parity.py` يشغّل المسار كاملاً (قاعدة عميل ← محرك الرفع ← سحابة ←
سحب المدير) ويقارن كل اسم وترتيبه وكل إعداد وكل عمود في كل حركة — وجهاز المدير بتوقيت
مختلف — ثم يعيد المقارنة بعد تعديل وحذف وإضافة عند العميل. و`supabase/tests/30_admin_mirror_tests.sql`
يختبر دوال السحابة نفسها.

**للتحديث على Supabase:** شغّل `supabase/16_admin_mirror_parity.sql` (أو `INSTALL_ALL.sql` كاملاً).
البرامج القديمة تبقى تعمل معه، والبرنامج الجديد يعمل مع سحابة لم تُحدَّث (يتخطّى الإعدادات فقط).

### 🎨 الواجهة الجديدة لبرنامج سطح المكتب (المرحلة الرابعة)

تعديل بصري فقط — **لا تغيير في أي حساب أو بيانات أو مزامنة**، ومنطق الدخول (`try_login`) كما هو.

| الجزء | ما تغيّر |
|---|---|
| **شاشة الترحيب** | تفتح بملء الشاشة: خلفية ليلية متدرّجة، هالة ذهبية، أمواج تتحرّك باستمرار يعلوها خطّا ذهب، ذرّات ذهب تصعد وتتلألأ، الشعار يظهر تدريجياً وحوله حلقات تموّج، ثم «مرحباً بك» وشريط تجهيز. بعد ٣ ثوانٍ (أو بأي ضغطة/نقرة) يصعد الشعار وتنزلق لوحة الدخول |
| **الدخول بلا لوحة** | الشعار والحقول تطفو على المشهد نفسه بلا بطاقة ولا إطار، وتظهر صاعدةً جزءاً بعد جزء: حقلان كبيران بحافة ذهبية للحقل النشط، زر إظهار كلمة المرور 👁، تنبيه Caps Lock، «تذكّر اسم المستخدم» (الاسم فقط — كلمة المرور لا تُحفظ)، Enter ينقل ثم يدخل. خطأ الدخول: اهتزاز خفيف ورسالة واضحة. التحقق عبر الإنترنت في الخلفية فلا تتجمّد الأمواج |
| **بعد الدخول: لا إغلاق ولا نوافذ** | كانت شاشة الدخول تُغلق ثم تظهر نافذة تجهيز ثم نافذة النظام. الآن: الحقول تختفي، الأمواج ترتفع طبقة بعد طبقة حتى تغمر الشاشة، والشعار ينزل للمنتصف مع «أهلاً بك» واسم المصنع، ورفع البيانات يظهر تحته. يُبنى النظام خلف هذا الإطار ثم يظهر فوقه **بالإطار نفسه** (مطابق بكسلاً ببكسل) ويغلق شاشة الدخول تحته، ثم تنحسر الأمواج كاشفةً الرئيسية من أعلاها، وتُعدّ أرقامها من الصفر وتلمع أزرار الشريط بالتتابع. أي خلل في الانتقال يُظهر النظام مباشرة، ومراقب زمني يضمن ألا يبقى الغطاء أبداً |
| **نظام تصميم موحّد** | لوحة ألوان واحدة (ذهبي/كحلي/أخضر/أحمر) تُطبَّق تلقائياً على كل الأزرار والنصوص، والألوان الباهتة تُصحَّح حسب الخلفية لتبقى مقروءة. خط **Cairo** العربي مرفق مع البرنامج (رخصة OFL المفتوحة) |
| **الشريط الجانبي** | بطاقات أوضح، اختصار **Ctrl+1…6** بجانب كل شاشة، **Esc** للرئيسية، وحجم الخط يتكيّف ليظهر اسم كل شاشة كاملاً |
| **الرئيسية** | تحية حسب الوقت والتاريخ بالعربي + أربع بطاقات لحظية للفترة: المبيعات/الصادر، الوارد، الخياس، عدد الحركات |
| **رأس كل شاشة** | أيقونة، اسم الشاشة ووصف قصير، شارات الفترة والخزينة والرصيد، وزر الرجوع للرئيسية |
| **الشاشات الست** | أزرار المراحل والأقسام تُبرز المختار بالذهبي، بطاقات بيضاء بحواف خفيفة، لوحة «ملخّص الإنتاج» في صناديق المصنع، وإصلاح خطأ كان يقصّ محتوى شاشة المبيعات |

**الإثبات:** `test_login_screen.py` (ضمن `run_all_checks.py` للنسختين) يتحقق أن الحقول بلا لوحة، وأن كلمة
المرور لا تُحفظ، وأن الترحيب لا يحبس المستخدم، وأن شاشة الدخول لا تُغلق قبل ظهور النظام، وأن الغطاء يُزال
حتى لو فشل الحساب الأول، وأن الخط مرفق ويُضمَّن في exe. وجُرّب المسار كاملاً فعلياً (دخول خاطئ ثم صحيح
مع رفع بيانات) بلقطات متتابعة.

### 🧮 الدفعة الخامسة: شاشات أوسع للجداول وإصلاحات محاسبية

| الموضع | ما كان | الآن |
|---|---|---|
| **الموردون ← القيود اليومية** | قيد يومي باسم مورد (مثلاً ١٠ جم) يظهر في كشف حسابه **ولا يظهر في جدول الموردين** — دالة الرصيد لم تكن تحسب القيود إطلاقاً | الرصيد يشمل القيود المدينة والدائنة ومطابق لكشف الحساب. ونقرتان على المورد تفتحان كشفه، والقيد على اسم غير مسجّل يطلب تأكيداً (خطأ كتابة لا يُنشئ حساباً وهمياً) |
| **المبيعات** | تمرير للشاشة وتمرير للجدول، وأزرار موزّعة أعلى الجدول وأسفله | الشاشة ثابتة والتمرير للجدول وحده؛ الخانات وزر «إضافة سطر» أعلاها، وأسفل الجدول شريط الترحيل وحده. أول عمود بلا عنوان فيه ✏️ تعديل و🗑️ حذف، وعمود «الصافي» حُذف |
| **تعديل سطر المبيعات** | نافذة عمودية **تُسقط خياس البوليش وخياس المركب** من السطر عند الحفظ، والحذف/التعديل في العرض التنازلي يصيب **سطراً آخر** | نافذة أفقية بكل خانات الصف تحفظها كلها مع الوزن القائم/المقيد لحظياً، والحذف بتأكيد ويصيب السطر الصحيح دائماً |
| **مراحل التصنيع** | عنوان نصي طويل، وثلاثة صفوف إدخال متفرقة تقتطع من الجدول | شريط الأقسام أعلى الشاشة، وبطاقة إدخال بصف واحد لكل قسم، والجدول يأخذ بقية الشاشة |
| **الكاستنج** | لا خانة لمسترجع الأشجار | خانة وعمود «مسترجع الأشجار» بعد القبض: خياس الصف = الصرف − القبض − المسترجع. يُسجَّل وارداً باسم «مسترجع كاستنج» ورقم الصف — المسار نفسه الذي يخصمه صندوق الخياس وتضيفه الخزينة ويقرؤه الويب، فتتطابق كل الأرقام بلا نوع حركة جديد |
| **المصنعون والمركبون** | الكشف **يستبدل** قيمة الصف بآخر حركة من النوع نفسه بينما صندوق الخياس **يجمعها** | الكشف يجمع (في الموضعين). وقاعدة الصف والخانة تُعرض قبل الترحيل: ما سيُسجَّل وما هو موجود في الصف فلا يُكرَّر، ورقم تشغيل مختلف لصفٍّ قائم يُرفض |
| **الخسائر** | نص تفصيلي صغير يزحم لوحتي المصنعين والمركبين | بطاقات نظيفة: الحالي والمُقفل جنباً إلى جنب، وكل الأدوات في شريط واحد أعلى الشاشة |
| **الرئيسية** | الشعار ذهبي باهت | الشعار بالأزرق على الأبيض متناسقاً مع الشريط الجانبي |

**الإثبات:** `test_screens_v3.py` (ضمن `run_all_checks.py` للنسختين) يشغّل الدوال الحقيقية: رصيد مورد بقيد يومي،
مسترجع الأشجار (خياس الصفوف = خياس الصندوق)، قاعدة الصف والخانة، وجمع قيم الكشف. وجُرّب سيناريو العميل كاملاً
فعلياً: مورد جديد ← قيد ١٠ جم ← يظهر في جدول الموردين مدين ١٠.

---

## رابعاً: الإضافات

| الإضافة | الفائدة |
|---|---|
| **شاشة «سجل التعديلات»** (للمدير داخل حساب العميل) | من أضاف/عدّل/حذف ماذا ومتى، ومن الويب أم من البرنامج — والحذف محفوظ كاملاً لإمكانية الإرجاع |
| **رصيد أول المدة في كشف الحساب** | كشف محاسبي صحيح لا يبدأ من الصفر كل شهر |
| **إيقاف/تفعيل حساب العميل** من لوحة المدير | كان في قاعدة البيانات بلا زر |
| `supabase/14_security_hardening.sql` | تحصين شامل + **تقرير** بكل دالة ما زالت متاحة لـ anon |
| `tools/build_install_all.py` | `INSTALL_ALL.sql` يُولَّد من الملفات المرقّمة — لا تعديل يدوي ولا نسخ متباينة |
| **اختبارات قاعدة البيانات** (`supabase/tests/`) | تشغّل التثبيت مرتين ثم تختبر العزل بين المصانع، صلاحيات anon، المحاسبة، القفل، حد محاولات الدخول، سجل التدقيق |
| **دفتر الخزينة للفترات** (البرنامج + الويب) | التقرير الشهري = الشريط = كشف الخزينة، وكل فترة تبدأ برصيد نهاية سابقتها |
| **اختبارات Flutter** (`test/`) — ١٩ اختباراً | منطق الفواتير، النماذج، الفترات، سجل التدقيق، وتوليد PDF عربي فعلياً |
| **اختبار سلوكي لمحرك المزامنة** (`test_cloud_sync_engine.py`) | يشغّل المحرك فعلاً بدل البحث عن نصوص في الكود |
| **CI على GitHub** (`.github/workflows/ci.yml`) | كل دفعة تُفحص آلياً: تحليل وبناء واختبارات الويب، تثبيت SQL واختباراته على PostgreSQL، فحوص نسختي البرنامج وأداة الترحيل |

---

## خامساً: تشغيل الفحوص محلياً

```bash
cd jadeite_web
flutter analyze && flutter test            # تطبيق الويب
python3 tools/check_project.py              # فحص بنيوي للويب
python3 tools/build_install_all.py --check  # INSTALL_ALL مطابق للملفات المرقّمة
python3 tools/check_sql.py
bash supabase/tests/run_sql_tests.sh        # يحتاج PostgreSQL (PGHOST/PGUSER…) — لا تشغّله على Supabase الحقيقي
cd desktop_sync && python3 run_all_checks.py && python3 run_all_checks.py rageh-CLIENT.py
cd ../migration && python3 test_migration.py
```

---

## سادساً: توصيات متبقية (خارج ما يمكن إصلاحه من هذا المستودع)

1. **دوال النظام القديم** في قاعدتك الحية غير موجودة في هذه الملفات فلم أستطع مراجعتها:
   `verify_client_login`، `check_client_can_edit`، `upload_backup`، **`download_backup`**،
   `verify_sub_admin_login`، وجداول `clients` / `db_backups` / `sub_admins`. البرنامج
   يستدعي `download_backup(p_client_id)` بمفتاح anon — **إن كانت لا تتحقق من كلمة مرور أو
   رمز، فأي شخص يعرف معرّف عميل ينزّل نسخة قاعدته كاملة.** راجع تعريفها (تظهر في تقرير
   `14_security_hardening.sql`) وأرسلها لي لأحصّنها.
   بعد تحديث كل العملاء لنسخة تستخدم `client_login_full`، اسحب `verify_client_login` من anon
   (لا حد لمحاولات التخمين فيها).
2. **كلمات مرور العملاء** مخزّنة `sha256` بلا ملح (salt)، والتحقق يقارن البصمة نفسها — فالبصمة
   تعمل ككلمة مرور. الحل الجذري: نقل العملاء لـ Supabase Auth (الكود جاهز في
   `supabase_api.py` و`login_screen.py`).
3. **صناديق الخياس الديناميكية** (المضافة من البرنامج): صارت تدخل في رصيد الخزينة وتقرير
   الفترات في الويب (`tenant_khayas_boxes` يقرأها من `category_raw`). المتبقي: شاشة
   «صناديق الخياس» في الويب تعرض الصناديق الأربعة الثابتة فقط.
4. **تعديلات المدير من الويب لا تنزل للعميل** (قرار الاتجاه الواحد) — تغيّر نسخة السحابة فقط،
   وقد يكتب العميل فوقها. الشريط يوضّح ذلك الآن. إن أردت التعديل عن بُعد مستقبلاً فيجب أن
   يمر بموافقة داخل برنامج العميل.
5. ملفات `patch_client_app_v*.py` (٣٦ ملفاً) سجل تاريخي لتعديلات سابقة ولا يستخدمها شيء —
   يمكن نقلها لمجلد أرشيف.
